[project]
name = "cafe-sales-pipeline"
version = "0.1.0"
description = "ETL pipeline for dirty cafe sales data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
]
