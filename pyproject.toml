[project]
name = "cafe-sales-pipeline"
version = "0.1.0"
description = "ETL pipeline for dirty cafe sales data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]
